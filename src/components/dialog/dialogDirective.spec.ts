'use strict';

import * as ng from 'angular';
describe('uifDialog: <uif-dialog />', () => {
    let elem: ng.IAugmentedJQuery;

    beforeEach(() => {
        angular.mock.module('officeuifabric.core');
        angular.mock.module('officeuifabric.components.dialog');
    });

    beforeEach(inject());

})