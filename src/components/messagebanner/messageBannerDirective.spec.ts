'use strict';

describe('messageBannerDirective: <uif-message-banner />', () => {
    beforeEach(() => {
        angular.mock.module('officeuifabric.components.messagebanner');
    });

    it('should be able to render', (): void => {
        return;
    });
    it('should be able to render text message', (): void => {
        return;
    });
    it('should be able to render ui-content message', (): void => {
        return;
    });
    it('should be able to expand', (): void => {
        return;
    });
    it('should be able to be closed', (): void => {
        return;
    });
    it('should be able to render action text', (): void => {
        return;
    });
    it('should be able to call action callback', (): void => {
        return;
    });
    it('should be able to call close callback', (): void => {
        return;
    });
    it('should be able to be resized', (): void => {
        return;
    });
});
